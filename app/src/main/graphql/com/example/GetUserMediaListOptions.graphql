query GetUserMediaListOptions($id: Int!) {
    user:User(id: $id) {
        mediaListOptions {
            scoreFormat
            rowOrder
            useLegacyLists
            sharedTheme
            sharedThemeEnabled
        }
    }
}